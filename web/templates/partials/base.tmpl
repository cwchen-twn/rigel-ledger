{{define "public:base"}}
<!doctype html>
<html lang="{{.Lang}}" data-theme="{{.Theme}}">
    <head>
        <title>{{block "title" .}}RigelLedger{{end}}</title>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="robots" content="noindex, nofollow">
        <link rel="icon" type="image/x-icon" href="/static/favicon.ico?version={{.Version}}">
        <script nonce="{{.Nonce}}">
// Resolve the theme before first paint so a dark-mode user never sees a white flash.
(function () {
  var t = document.documentElement.dataset.theme;
  var dark = t === "dark" || (t === "system" && window.matchMedia("(prefers-color-scheme: dark)").matches);
  document.documentElement.classList.toggle("dark", dark);
})();
window.__APP_CONFIG__ = { version: "{{.Version}}" };
        </script>
        {{- /* Hashed names from the Vite manifest: a new build is a new URL. */}}
        {{range .Assets.CSS}}<link rel="stylesheet" href="{{.}}">
        {{end}}{{with .Assets.JS}}<script src="{{.}}" defer nonce="{{$.Nonce}}"></script>{{end}}
        {{block "head" .}}{{end}}
    </head>
    <body>
        {{block "main" .}}{{end}}
    </body>
</html>
{{end}}
